// storage.rules
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access for local testing
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}